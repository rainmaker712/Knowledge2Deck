{
  "name": "knowledge2deck-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "slides": "node scripts/build-slides.mjs",
    "prebuild": "node scripts/build-slides.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test \"src/lib/**/*.test.js\""
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.5.0",
    "@tailwindcss/vite": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
